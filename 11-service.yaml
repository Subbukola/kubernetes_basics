apiVersion: v1 
kind: Service
metadata:
  name: robo-secret
  labels:
    version: 0.1
    project: roboshop
spec:
  selector:
    env: dev
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP




