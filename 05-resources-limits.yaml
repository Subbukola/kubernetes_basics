apiVersion: v1
kind: Pod
metadata:
  name: resources-demo
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "50Mi"
        cpu: "100m"
      limit:
        memory: "75Mi"
        cpu: "1000m"